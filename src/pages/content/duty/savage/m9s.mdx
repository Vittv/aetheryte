---
date: 2026-06-11
---

import contentImg from "../../../../assets/images/m9s.png"
import data from "../../../../../data/duty/savage/m9s.json"

# Vamp Fatale (Savage)

<p className="frontmatter">Last updated: <span className="content-date">{frontmatter.date}</span></p>

<img src={contentImg} alt="m9s banner" />

## Party Finder Description

```markdown
Hector | TF | Static Aetherletting
```

  - **Hector**: We are using strats from the Hector guide.
  - **TF**: Toxic Friends raidplan.
  - **Static Aetherletting**: Static star puddle locations for Aetherletting.

## Resources

### Markers

```json
{
  "Name": "m9s",
  "MapID": 1069,
  "A": { "X": 100.0, "Y": 0.0, "Z": 90.0, "ID": 0, "Active": true },
  "B": { "X": 110.0, "Y": 0.0, "Z": 100.0, "ID": 1, "Active": true },
  "C": { "X": 100.0, "Y": 0.0, "Z": 110.0, "ID": 2, "Active": true },
  "D": { "X": 90.0, "Y": 0.0, "Z": 100.0, "ID": 3, "Active": true },
  "One": { "X": 90.0, "Y": 0.0, "Z": 90.0, "ID": 4, "Active": true },
  "Two": { "X": 110.0, "Y": 0.0, "Z": 90.0, "ID": 5, "Active": true },
  "Three": { "X": 110.0, "Y": 0.0, "Z": 110.0, "ID": 6, "Active": true },
  "Four": { "X": 90.0, "Y": 0.0, "Z": 110.0, "ID": 7, "Active": true }
}
```

### Unlock

- <BulletLink href={data.unlock}>Wiki</BulletLink>

### Videos

{data.videos.map((video) => (
  <VideoEmbed key={video.url} src={video.url} title={video.label} />
))}

### Additional Resources

<ul>
{data["additional-resources"].map((resource) => (
  <li key={resource.url}>
    <BulletLink href={resource.url}>{resource.label}</BulletLink>
  </li>
))}
</ul>
